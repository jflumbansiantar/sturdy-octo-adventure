# syntax=docker/dockerfile:1

# --- Build stage ---
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

COPY ["src/PortfolioOS.AdminWeb/PortfolioOS.AdminWeb.csproj", "src/PortfolioOS.AdminWeb/"]
RUN dotnet restore "src/PortfolioOS.AdminWeb/PortfolioOS.AdminWeb.csproj"

COPY src/PortfolioOS.AdminWeb/ src/PortfolioOS.AdminWeb/

WORKDIR /src/src/PortfolioOS.AdminWeb
RUN dotnet publish -c Release -o /app/publish --no-restore

# --- Runtime stage: static files served by nginx ---
FROM nginx:alpine AS final
COPY --from=build /app/publish/wwwroot /usr/share/nginx/html
COPY src/PortfolioOS.AdminWeb/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
